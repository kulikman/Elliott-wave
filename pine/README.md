# Elliott Wave Brain — Pine Script индикаторы

Индикаторы для TradingView, реализующие методологию Нили (NeoWave) уровень за уровнем.
Каждый уровень опирается на правила из `brain-output/indicator-spec/spec_vN.json`.

## Probability Overlay v0

Файл: `pine/ewb_probability_overlay_v0.pine`

Это отдельный MVP-слой для цели Антона: быстро видеть вероятность дальнейшего
движения акции и торговое решение `BUY / SELL / SKIP` прямо на графике.

**Что делает:**
- строит подтверждённые pivot-окна без look-ahead: сигнал появляется только после
  `rightBars` подтверждения;
- ищет `flat` и `double_corr` как торговые паттерны v0;
- показывает `Action now` (`BUY / SELL / WAIT`) отдельно от исторического `Last signal`;
- показывает силу `A/B/C`, `P(win)`, `EV`, stop и target;
- показывает `Conf / N`, чтобы отличать сильную вероятность на большой выборке от
  сигнала с маленьким числом наблюдений;
- калибрует `double_corr long` и `double_corr short` отдельно, потому их выборки и EV
  отличаются;
- считает остаточный `Remain R:R` от текущей цены и переводит поздний вход в `WAIT`;
- по умолчанию работает в `Market mode = Stocks` и переводит crypto/unsupported symbols
  в `WAIT`, потому калибровка v0 предназначена для акций;
- показывает `impulse/triangle` как `Skip` / no-trade context;
- переводит отработанные, выбитые или устаревшие сигналы в `WAIT`, чтобы старый
  `Last signal` не выглядел как новая рекомендация;
- использует калибровку Probability Model v0 из `brain-output/indicator-spec/probability-model-v0.md`
  и `probability_calibration_v0.json`.

**Честные ограничения v0:**
- это overlay, а не полный Neely Core;
- pivot-логика упрощена относительно Python `detect_monowaves + match_figures`;
- вероятности заданы input-параметрами, а не динамически загружаются из JSON
  (Pine не умеет читать локальные файлы);
- `double_corr` в Pine v0 является приближённым detector-условием; даже при высоком
  P(win) он получает низкую confidence, пока выборка маленькая.

**Как запустить:**
1. Открой TradingView → Pine Editor.
2. Вставь содержимое `pine/ewb_probability_overlay_v0.pine`.
3. Add to chart.
4. Для 1h акций оставь дефолтные вероятности; для других ТФ сначала сверяй с Python daily report.

**Как сверять с Python:**
1. Обнови daily report: `python3 python/scripts/daily_report.py`.
2. Сгенерируй checklist: `python3 python/scripts/pine_parity_checklist.py`.
3. Открой `docs/pine_parity_checklist.md` и сравни значения Pine с Python по тикерам.

## Принцип построения

Pine Script v6 **не имеет рекурсии** и имеет лимиты на циклы/массивы, поэтому
полный рекурсивный волновой счёт Нили реализуется не «в лоб», а послойно —
снизу вверх. Каждый слой детерминирован и тестируем отдельно.

## Дорожная карта (уровни Нили → модули Pine)

Источник правил: `brain-output/indicator-spec/spec_v2.0.json` — 126 правил
с формализованным `constraint` (псевдокод). Каждый Pine-слой кодируется строго
по constraint с трассировкой `// AKU-XXXX`.

| Уровень | Глава Нили | Модуль | Статус | Блокер |
|---------|-----------|--------|--------|--------|
| 1 — Моноволны | Гл.2 | `ewb_monowaves_mtf.pine` | ✅ есть | — |
| 2 — Определитель Правил | Гл.3 | `ewb_monowaves_mtf.pine` (в нём же) | 🟡 классификация Пр.1-7+Условие готова | присвоение единой метки :5/:F3/... нужны структ. списки Правил 4-7 (стр.3-44+) |
| 3 — Группировка/Серии | Гл.4 | `ewb_monowaves_mtf.pine` (в нём же) | 🟡 базовое распознавание фигур по Сериям | точное различение Зигзаг/Импульс, Треуг/Терминал нужны Индикаторы положения |
| 4 — Подтверждение фигур | Гл.5, 6 | `ewb_patterns.pine` | 🟡 spec готов | зависит от Ур.3 |
| 8 — Каналы + Фибоначчи | Гл.12 | `ewb_channels_fib.pine` | 🟢 детерминированная геометрия | — |
| 9 — Торговое решение | — | `ewb_strategy.pine` | ⛔ | зависит от Ур.4 |

**Сделано (Уровень 2 в `ewb_monowaves_mtf.pine`):** для каждой моноволны вычисляется
`m2/m1 → Правило 1-7` (Определитель Правил, AKU-0141) и `m0/m1 → Условие a-f` (AKU-0142),
метка «П{N}{буква}» выводится на графике. Это структурная классификация Нили.

**Сделано (Уровень 3 в `ewb_monowaves_mtf.pine`):** цепочка базовых структур (:5/:3)
сопоставляется скользящим окном со Структурными Сериями — распознаются и подсвечиваются
рамкой Импульс (5-3-5-3-5), Зигзаг (5-3-5), Плоская (3-3-5), Треугольник/Терминал
(3-3-3-3-3). Приоритет 5-элементных шаблонов (правило «Зигзаг или Импульс?» AKU-0166).

**Честные ограничения распознавания (v1):**
- Используется базовая структура :5/:3 (мой классификатор даёт только её). Точные метки
  :F3/:c3/:L5 и позиционные различия требуют логики Индикаторов положения (Гл.3, стр.3-60+,
  ещё не извлечена) → Зигзаг vs хвост Импульса и Треугольник vs Терминал не различаются.
- Правила 6-7 (откат m1 > 161.8%) дают структуру «?» → разрыв в цепочке, фигура не матчится.

**Следующий шаг:** Индикаторы положения (стр.3-60…3-68) для точного присвоения F/L/c
и однозначного различения фигур; затем подтверждение фигур правилами Гл.5-6 (spec готов).

## ewb_monowaves_mtf.pine (Уровень 1)

**Что делает:**
- Идентифицирует моноволны (движения между сменами направления) reversal-ZigZag методом
- Нумерует их `m0, m1, m2...` на текущем ТФ
- Параллельно строит моноволны старшего ТФ (`M0, M1...`) — **MTF-синхронизация**
- Порог разворота настраивается по ATR или в процентах

**Как запустить:**
1. Открой TradingView → Pine Editor
2. Вставь содержимое `ewb_monowaves_mtf.pine`
3. Add to chart
4. Настрой старший ТФ и порог разворота под свой инструмент

**Реализованные правила из базы AKU:**
- **AKU-0003 — Правило подобия и баланса** (verified + formalized): для каждой пары
  смежных моноволн проверяется `min >= max/3` по цене ИЛИ времени. Если правило
  нарушено → жёлтый маркер `≠` на границе: моноволны разного Порядка, кандидат
  на сжатие (compaction). Это мост от Уровня 1 к Уровню 3 (группировка).

**Ограничения v1 (честно):**
- HTF-пивоты привязываются к bar_index закрытия HTF-бара → возможен небольшой сдвиг
- Это Уровень 1: классификация `:3`/`:5` и распознавание фигур — следующие уровни
- Reversal-порог — упрощение строгого определения моноволны Нили; уточняется на Уровне 2
- Правило подобия пока маркируется визуально; автоматическое сжатие — Уровень 3

## Что нужно чтобы двигаться к Уровню 2+

1. **Пополнить API-кредиты Anthropic** → доделать:
   - `python tools/formalize_aku.py` — формализация 122 правил в constraint
   - OCR глав 2-4 (моноволны, предварительный анализ, группировка)
2. После формализации `spec_v2.json` будет содержать псевдокод constraint
   → можно кодировать Уровни 2-4 без галлюцинаций
