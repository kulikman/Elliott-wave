id: AKU-0168
type: rule
strength: conditional
status: verified
topic: structural-labels
subtopics:
- length-ratio-rules
- monowave
statement_ru: 'Правило 5 (100% ≤ m2/m1 < 161.8%): структурный список m1 = {:F3, :c3,
  :5, :L5, (:L3)}. Вероятная метка :F3.'
statement_en: null
source:
  book_id: neely-mwe-1990
  page: 3-51
  chapter: 'Глава 3: Предварительный анализ'
  section: Неформальные Правила логики / структурные списки
  figure_id: null
  verbatim_quote:
    text: Правило 5 {:F3/:c3/:5/:L5/(:L3)}
    language: ru
formalization:
  status: draft
  applies_when: '# Правило 5 из неформальных правил логики / структурных списков

    # Применяется когда отношение длины m2 к m1 попадает в диапазон [100%, 161.8%)

    ratio_m2_m1 = abs(m2.length) / abs(m1.length)

    ratio_m2_m1 >= 1.0 AND ratio_m2_m1 < 1.618

    '
  constraint: '# Структурный список для m1 при выполнении Правила 5:

    # m1 может иметь одну из следующих структурных меток:

    #   :F3  — Flat (тройка), наиболее вероятная

    #   :c3  — corrective тройка

    #   :5   — пятёрка (импульс или терминал)

    #   :L5  — пятёрка с удлинением (L = Long/Large)

    #   (:L3) — тройка с удлинением (в скобках = менее вероятная / условная)


    allowed_labels_m1 = ['':F3'', '':c3'', '':5'', '':L5'', ''(:L3)'']

    structure(m1) IN allowed_labels_m1


    # Наиболее вероятная (primary) метка:

    primary_label_m1 = '':F3''


    # Приоритет меток (от наиболее к наименее вероятной):

    # 1. :F3  (primary)

    # 2. :c3

    # 3. :5

    # 4. :L5

    # 5. (:L3)  — наименее вероятная, условная (обозначена скобками)

    '
  formalization_notes: 'Правило определяет допустимые структурные метки для моноволны
    m1

    на основании соотношения длин m2/m1 (Правило 5: от 100% до 161.8%).

    Скобки вокруг :L3 в оригинале означают пониженную вероятность данной метки.

    Точный порядок приоритета меток (кроме primary :F3 и условной (:L3))

    не указан явно в verbatim_quote — порядок в списке принят как порядок приоритета.'
aliases: []
related_aku: []
contradicts_aku: []
extends_aku: null
supersedes_aku: null
created_by: claude
created_at: '2026-06-02T19:20:47Z'
review_notes: Извлечено Claude через прямое чтение PDF (Гл.3, структурные списки Правил
  4-7).
requires_review: false
